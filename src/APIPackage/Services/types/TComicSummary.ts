export type TComicSummary = {
    resourceURI?: string; // (string, optional): The path to the individual comic resource.,
    name?: string; // (string, optional): The canonical name of the comic.
}
