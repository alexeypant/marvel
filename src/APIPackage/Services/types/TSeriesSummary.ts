export type TSeriesSummary = {
    resourceURI?: string; // (string, optional): The path to the individual series resource.,
    name?: string; // (string, optional): The canonical name of the series.
}
